// RemoteMessagePort

INFOPLIST_FILE = RemoteMessagePort/Info.plist
WRAPPER_EXTENSION = xpc
PRODUCT_BUNDLE_IDENTIFIER = ${REMOTE_MESSAGE_PORT_BUNDLE_ID}
PRODUCT_NAME = ${REMOTE_MESSAGE_PORT_PRODUCT_NAME}
