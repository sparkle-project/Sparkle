// TemporaryDownloader

INFOPLIST_FILE = TemporaryDownloader/Info.plist
WRAPPER_EXTENSION = xpc
PRODUCT_BUNDLE_IDENTIFIER = ${TEMPORARY_DOWNLOADER_BUNDLE_ID}
PRODUCT_NAME = ${TEMPORARY_DOWNLOADER_BUNDLE_ID}
CODE_SIGN_ENTITLEMENTS = TemporaryDownloader/${TEMPORARY_DOWNLOADER_BUNDLE_ID}.entitlements
