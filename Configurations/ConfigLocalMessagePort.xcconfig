// LocalMessagePort

INFOPLIST_FILE = LocalMessagePort/Info.plist
WRAPPER_EXTENSION = xpc
PRODUCT_BUNDLE_IDENTIFIER = ${LOCAL_MESSAGE_PORT_BUNDLE_ID}
PRODUCT_NAME = ${LOCAL_MESSAGE_PORT_PRODUCT_NAME}
